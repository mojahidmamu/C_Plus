#include <iostream>
#include <algorithm> // for build-in-function..
using namespace std;
int main()
{
    int a, b;
    cin >> a >> b;
    cout << min(a, b) << endl; // min = build-in-function
    cout << max(a, b) << endl; // max = build-in-function
    return 0;
}