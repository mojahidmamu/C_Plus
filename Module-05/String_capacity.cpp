#include <bits/stdc++.h>
using namespace std;
int main()
{
    // s.size() -> returns the size of the string.
    string s = "Mojahid";
    cout << s.size() << endl; // size for 
    cout << s.max_size() << endl; 
    cout << s.capacity() << endl; 
    // cout  << s.clear() << endl;
    cout  << s.empty() << endl;
    // cout  << s.resize(12) << endl;
    return 0;
}
