#include <bits/stdc++.h>
using namespace std;
int main()
{
     string s = "Abdullah";
    //  string s2 = "Mojahid";
    //  s.append(s2);
    // s.push_back('A');
    // s.pop_back();

    // s.erase(3, 2); // delete from index-3 and only 2 element
    // s.replace(3, 2, "MA"); // replace from index-3 and 2 element and replace 'MA'
    // s.insert(8,"Mojahid"); // insert a full string from index-8


     cout << s << endl;
    return 0;
}
