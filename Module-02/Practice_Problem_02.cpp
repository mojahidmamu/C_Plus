#include <bits/stdc++.h>
using namespace std;
int main()
{
    int N;
    cin >> N;
    int A[N];
    for (int i = 0; i < N; i++)
    {
        cin >> A[i];
    }

    int M;
    cin >> M;
    int B[M - N];
    for (int i = 0; i < M - N; i++)
    {
        cin >> B[i];
    }

    

    return 0;
}
